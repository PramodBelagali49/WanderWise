<%- layout("/layouts/boilerplate.ejs") %>
<body>
    <h2>Listing Details:</h2>
    <ul>
        <li><%= listingData.title %></li>    
        <li><%= listingData.description %></li>     
        <li> &#8377; <%= listingData.price.toLocaleString("en-IN") %> </li>   
        <li><%= listingData.location %></li>    
        <li><%= listingData.country %></li>  
    </ul>

    <a href="/listings/<%=listingData._id%>/edit">Edit the Listing</a>      <br> <br>
    <form action="/listings/<%=listingData._id%>?_method=DELETE" method="POST" onsubmit="return confirmation()" >
        <button type="submit">Delete the Listing</button>
    </form>

    <script>
        function confirmation(){
            return confirm("Are you sure to delete the listing ? ");
        }
    </script>
</body>